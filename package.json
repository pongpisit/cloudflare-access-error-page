{
  "name": "cloudflare-custom-pages",
  "version": "1.0.0",
  "description": "Cloudflare custom pages for SWG and user coaching",
  "main": "main.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "node src/build.js",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "swg",
    "block-page",
    "coaching"
  ],
  "author": "Zero Security Corp",
  "license": "PROPRIETARY",
  "devDependencies": {
    "wrangler": "^4.59.3"
  }
}
