{
  "name": "cloudflare-access-error-page",
  "version": "1.0.0",
  "description": "Cloudflare One (Zero Trust) Access custom error page that tells each user why they were denied",
  "main": "main.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "node src/build.js",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "keywords": [
    "cloudflare",
    "cloudflare-one",
    "zero-trust",
    "access",
    "workers",
    "block-page",
    "error-page"
  ],
  "author": "Zero Security Corp",
  "license": "PROPRIETARY",
  "cloudflare": {
    "bindings": {
      "BEARER_TOKEN": {
        "description": "Create an API token with **Account → Access: Apps and Policies → Read**, **Account → Access: Audit Logs → Read**, **Account → Zero Trust: Devices → Read**, and **Device Posture → Read** permissions. See the README for details."
      }
    }
  },
  "devDependencies": {
    "wrangler": "^4.59.3"
  }
}
